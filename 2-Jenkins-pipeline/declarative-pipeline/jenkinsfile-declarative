pipeline {
    agent any
    
    stages {
        stage('Bmw') {
            steps {
                // Run build commands
                sh 'echo "Building project..."'
            }
        }
        
        stage('m series') {
            steps {
                // Run tests
                sh 'echo "Running tests..."'
            }
        }
        
        stage('M5') {
            steps {
                // Deploy step (dummy for now)
                sh 'echo "Deploying application..."'
            }
        }
    }
}
